/**
 * Ports shared by the development split, the local CLI host, and the
 * desktop-managed remote transport.
 */
export const DEFAULT_HOST_PORT = 4174;
export const DEFAULT_DEV_UI_PORT = 4174;
export const DEFAULT_DEV_HOST_PORT = 4175;
export const DEFAULT_SSH_REMOTE_PORT = 4177;
